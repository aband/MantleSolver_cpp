#include "efficient_recon.h"
// Compute sigma efficiently (make it faster)

int reconstruction::prepare(const vector<int>& insize, const MeshInfo& mi){

    size = insize;

    stencilPoly = Tensor<stencilpolynomial>(2);



    return 1;
}


