\section{Coupled Degenerate Darcy-Stokes System}
We consider a degenerate Darcy-Stokes coupled boundary-value problem in the dimensionless form:

Find two velocity-pressure potential pairs $(\check{\mathbf{v}}_r, \check{q}_l)$ and 
$(\check{\mathbf{v}}_s, \check{q})$ such that 
\begin{alignat}{2}
\label{eq:dimensionlessdarcy_flow_sc_bv}
		  &\check{\mathbf{v}}_r
		  + \phi^{1+\Theta}\nabla(\phi^{-1/2}\check{q}_l)
        &&= 0,\\
\label{eq:dimensionlessconsMass_flow_sc_bv}
		  &\phi^{-1/2}\nabla \cdot(\phi^{1+\Theta}\check{\mathbf{v}}_r)
        + \frac{1}{1-\phi}(\check{q}_l - \phi^{1/2}\check{q}) &&=0,\\
\label{eq:dimensionlessstokes_flow_sc_bv}
		  &\nabla \check{q} - \nabla \cdot \check{\tau}(\check{\mathbf{v}}_s) &&= -(1-\phi)\mathbf{n},\\
\label{eq:dimensionlesscompaction_flow_sc_bv}
		  &\nabla \cdot \check{\mathbf{v}}_s
        - \frac{\phi^{1/2}}{1-\phi}(\check{q}_l - \phi^{1/2}\check{q}) &&=0,
\end{alignat}
and
\begin{equation}\label{eq:bv}
		  \begin{alignedat}{2}
					 \phi^{1+\Theta}\check{\mathbf{v}}_r\cdot \pmb{\nu} &= g_{r} \quad &&\text{on} \hspace{0.2cm} \Gamma_{u,D}, \\
					 \check{\mathbf{v}}_s &= \mathbf{g}_{s} \quad &&\text{on} \hspace{0.2cm} \Gamma_{u,S}, \\
					 \check{q}_l &= \phi^{1/2}q_{l,0} \quad &&\text{on} \hspace{0.2cm} \Gamma_{i,D}, \\
					 \pmb{\tau}\cdot \pmb{\nu} - q &= \pmb{t}_0  \quad &&\text{on} \hspace{0.2cm} \Gamma_{i,S}.
		  \end{alignedat}
\end{equation}
Here, $\Gamma_{u,D}$ and $\Gamma_{u,S}$ denote the parts of the boundary, where essential boundary conditions 
are prescribed for 
the Darcy and the Stokes problems, respectively, 
while $\Gamma_{i,D}$ and $\Gamma_{i,S}$ represent the portions of the boundary, where natural boundary conditions 
are imposed.
We assume the essential and natural boundary conditions are non overlapping and covering
the entire boundary, i.e., $\Gamma_{u,D} \cup \Gamma_{i,D} = \partial \Omega, \, \Gamma_{u,D} \cap \Gamma_{i,D} = \O$ 
and $\Gamma_{u,S} \cup \Gamma_{i,S} = \partial \Omega, \, \Gamma_{u,S} \cap \Gamma_{i,S} = \O$.

To ensure well-posedness in the presence of degeneracy ($\phi=0$), we must verity that 
all terms scaled by $\phi^{-1/2}$ remain well-defined.
We expand the gradient term in equation~\eqref{eq:dimensionlessdarcy_flow_sc_bv}
and the divergence term in equation~\eqref{eq:dimensionlessconsMass_flow_sc_bv} as
\begin{equation}
		  \begin{aligned}
					 \phi^{1+\Theta} \nabla (\phi^{-1/2} \check{q}_l) &= \phi^{1/2+\Theta} \nabla \check{q}_l +  \phi^{\Theta-1/2} \check{q}_l \nabla \phi, \\
					 \phi^{-1/2} \nabla \cdot (\phi^{1+\Theta} \check{\mathbf{v}}_r) &=
					 \phi^{1/2+\Theta} \nabla \cdot \check{\mathbf{v}}_r + 
					 \phi^{\Theta - 1/2} \nabla \phi \cdot \check{\mathbf{v}}_r,
		  \end{aligned}
\end{equation}
and they are well-defined provided that
\begin{equation}\label{eq:phiboundness}
		  \phi^{\Theta - 1/2} \nabla \phi \in (L^{\infty}(\Omega))^2.
\end{equation}

We define a scaled Hilbert space tailored to the present formulation, within which
the scaled velocity $\check{\mathbf{v}}_r$ should lie in.
\begin{defn}
Consider a scaled Hilbert space:
\begin{equation}\label{eq:scaledHspace}
		  H_{\phi}(\text{div}; \Omega) = \Big\{ \mathbf{v} 
		  \in (L^2(\Omega))^2: \phi^{-1/2}\nabla \cdot (\phi^{1+\Theta} \mathbf{v})
		  \in L^2(\Omega) \Big\},
\end{equation}
which is a Hilbert space equipped with the inner product
\begin{equation}\label{eq:scaledinnerp}
		  (\mathbf{u}, \mathbf{v})_{H_\phi(\text{div};\Omega)} = 
		  (\mathbf{u}, \mathbf{v}) + 
		  (\phi^{-1/2} \nabla \cdot (\phi^{1+\Theta}\mathbf{u}), \phi^{-1/2} \nabla \cdot (\phi^{1+\Theta}\mathbf{v})).
\end{equation}
The normal trace on $\partial \Omega$ is well-defined as
\begin{equation}\label{eq:tracescaled}
		  \norm{\phi^{1/2+\Theta}\mathbf{v}\cdot \pmb{\nu}}_{H^{-1/2}(\partial \Omega)} < C_{\Omega} \Big\{ \norm{\mathbf{v}}_{L^2(\Omega)} + \norm{\phi^{-1/2}\nabla \cdot (\phi^{1+\Theta}\mathbf{v})}_{L^2(\Omega)} \Big\}
\end{equation}
		  subsequently, we have the space $H_\phi^{-1/2}(\text{div};\partial \Omega)$ understood as the image of the normal trace operator on $H_\phi(\text{div};\Omega)$.
\end{defn}
\subsection{The weak formulation}
We assume that the Dirichlet data
$\mathbf{g}_s \in (H^{1/2}(\Gamma_{u,S}))^2$, and understand 
$\tilde{\mathbf{g}}_s \in (H^1(\Omega))^2$ as a continuous extension from the
boundary into the domain.
%$\mathbf{g}_r \in (H^{1/2}_\phi(\text{div};\Gamma_{u,D}))^2$
%and . 
Similarly, we assume that $\phi^{-1/2} g_r \in H^{-1/2}_\phi(\partial \Omega)$
understood as the image of the scaled normal trace operator~\eqref{eq:tracescaled}.
Let
\begin{equation}
		  \phi^{1/2+\Theta}\mathbf{g}_r\cdot \pmb{\nu} = \phi^{-1/2}g_r \quad \text{on} \quad \partial \Omega.
\end{equation}
We view $\tilde{\mathbf{g}}_r$ as a continuous extension from the boundary into the domain.

The following energy spaces are defined for this scaled Darcy-Stokes coupled system 
\begin{equation}\label{eq:spaces}
		  \begin{aligned}
					 \mathbb{V}^\phi_D &= \Big\{\mathbf{v}\in H_{\phi}(\text{div};\Omega): \phi^{1/2+\Theta} \mathbf{v}\cdot \pmb{\nu} = 0 \hspace{0.15cm} \text{on}\hspace{0.15cm} \Gamma_{u,D}  \Big\},\\
					 \mathbb{U}^\phi_D &= \Big\{\mathbf{u}\in H_{\phi}(\text{div};\Omega): \phi^{1/2+\Theta} \mathbf{u}\cdot \pmb{\nu} = \phi^{-1/2}g_r \hspace{0.15cm} \text{on}\hspace{0.15cm} \Gamma_{u,D}  \Big\} = \tilde{\mathbf{g}}_r + \mathbb{V}^\phi_D,\\
					 \mathbb{W}_D      &= L^2(\Omega),\\
					 \mathbb{V}_S &= \Big\{\mathbf{v}\in (H^1(\Omega))^2 : \mathbf{v} = \mathbf{0} \hspace{0.15cm} \text{on}\hspace{0.15cm}  \Gamma_{u,S} \Big\},\\
					 \mathbb{U}_S &= \Big\{\mathbf{u}\in (H^1(\Omega))^2 : \mathbf{u} = \mathbf{g}_s \hspace{0.15cm} \text{on}\hspace{0.15cm}  \Gamma_{u,S} \Big\} 
					 = \tilde{\mathbf{g}}_s + \mathbb{V}_S,\\
		  \mathbb{W}_S &= L^2(\Omega),
		  \end{aligned}
\end{equation}
where each space is equipped with their natural norms.
%Here, $\tilde{\mathbf{g}}_r$ and $\tilde{\mathbf{g}}_s$ are finite energy lift 
%of Dirichlet data $\mathbf{g}_r$ and $\mathbf{g}_s$ respectively. 
%We understand the boundary values in the sense of Trace Theorem~\ref{thm:tracethm}
%To impose essential boundary conditoins, we assume that a function $\mathbf{g}_s = \check{\mathbf{v}}_{s,0} \hspace{0.2cm} \text{on} \hspace{0.2cm} \Gamma_{u,S}$ 
%and $\mathbf{g}_s = \mathbf{0} \hspace{0.2cm} \text{on} \hspace{0.2cm} \partial \Omega / \Gamma_{u,S}$.
%Then $\mathbf{g}_s \in (H^{1/2}(\partial \Omega))^2$ and
%extended it continuously from the boundary to the domain, so that
%the extension $\mathbf{g}_{s} \in (H^1(\Gamma_{u,S}))^2$ and $\norm{\mathbf{g}_{s,0}}_1 \leq C \norm{\mathbf{v}_{s,0}}_{1/2,\Gamma_{u,S}}$. 
%Similarly, we can define a bounded extension of $\check{\mathbf{v}}_r \in \mathbb{V}_D^\phi$ such that   

A compatibility condition is required to ensure overall mass conservation when essential boundary conditions 
are prescribed on the entire boundary $\partial \Omega$, i.e., when $\Gamma_{u,D} = \Gamma_{u,S} = \partial \Omega$. 
In this case, the prescribed Dirichlet data must satisfy the following compatibility condition:
\begin{equation}
		  \int_{\partial \Omega} (\phi^{1+\Theta}\mathbf{g}_r + \mathbf{g}_s) \cdot \pmb{\nu}\, ds = 0.
\end{equation}
In practice, we may not enforce this compatibility condition explicitly due 
to the pressence of natural boundary conditons.

The standard mixed form for the proposed coupled system reads as follows:
%Find $\check{\mathbf{v}}_r \in \mathbb{V}^{\phi}_D + \tilde{\mathbf{g}}_{r}$,
%$\check{q}_l \in \mathbb{W}_D$, $\check{\mathbf{v}}_s \in \mathbb{V}_S + \tilde{\mathbf{g}}_S$, and $\check{q}\in \mathbb{W}_S$ such that

Find $\check{\mathbf{v}}_r \in \mathbb{U}^{\phi}_D$,
$\check{q}_l \in \mathbb{W}_D$, $\check{\mathbf{v}}_s \in \mathbb{U}_S$, and $\check{q}\in \mathbb{W}_S$ such that
\begin{alignat}{2}
		  \label{eq:weak_coupled_1}
		  &(\check{\pmb{\tau}}(\bold{v}_{s}),\nabla \pmb{\psi}_s) - (\check{q}, \nabla \cdot \pmb{\psi}_s) = -((1-\phi)\bold{n}, \pmb{\psi}_s) &&\forall\pmb{\psi}_s \in \mathbb{V}_S,\\
		  \label{eq:weak_coupled_2}
					 &(\nabla \cdot \check{\bold{v}}_{s}, \omega)  + \Big(\frac{\phi^{1/2}}{1-\phi} (\check{q}_l - \phi^{1/2}\check{q}), \omega\Big) = 0 &&\forall \omega \in \mathbb{W}_S,\\
					 \label{eq:weak_coupled_3}
					 &(\bold{\check{v}}_{r},\pmb{\psi}_r)  - (\check{q}_{l},\phi^{-1/2} \nabla \cdot (\phi^{1+\Theta}\check{\bold{v}}_r)) = 0, &&\forall \pmb{\psi}_r \in \mathbb{V}_D^\phi,\\
					 \nonumber
					 &(\phi^{-1/2} \nabla \cdot (\phi^{1+\Theta}\check{\bold{v}}_r),\omega_f) + \Big(\frac{1}{1-\phi}(\check{q}_{l} - \phi^{1/2}\check{q}),\omega_f \Big) &&= 0 \\
					 \label{eq:weak_coupled_4}
		  &					 &&\forall \omega_f \in 
		  \mathbb{W}_D.
\end{alignat}

We state the well-posedness result of the scaled formulation directly as follows.
\begin{lem}\label{lem:ExistenceUniqueness}
		  (Existence and Uniqueness)

		  Assume the condition~$\eqref{eq:phiboundness}$ holds,
		  $0 \leq \phi \leq \phi^* <1$.
		  There exists a unique 
		  solution to the scaled formulation 
		  ~\eqref{eq:weak_coupled_1}--\eqref{eq:weak_coupled_4}, and it satisfies 
		  \begin{equation}
					 \begin{aligned}
					 \norm{\check{\mathbf{v}}_r}_{(L^2(\Omega))^2} + 
					 \norm{\phi^{-1/2}\nabla \cdot (\phi^{1+\Theta}\check{\mathbf{v}}_r)}_{(L^2(\Omega))^2} + 
					 \norm{\check{q}_l}_{(L^2(\Omega))^2} + 
					 \norm{\check{\mathbf{v}}_s}_{(H^1(\Omega))^2} + \norm{\check{q}}_{(L^2(\Omega))^2} \\
					\leq	C\big\{\lvert\rho_r \rvert +
								\norm{\tilde{\mathbf{g}}_r}_{(L^2(\Omega))^2} + 
								\norm{\phi^{-1/2}\nabla \cdot (\phi^{1+\Theta}\tilde{\mathbf{g}}_r)}_{(L^2(\Omega))^2}		+ \norm{\tilde{\mathbf{g}}_s}_{H^1(\Omega)}\big\}.
					 \end{aligned}
		  \end{equation}
\end{lem}
\begin{proof}
		  See~\cite{DegenerateDarcy}.
\end{proof}

\subsection{A scaled mixed finite element method}
We discretize the scaled mixed formulation using
two pairs of finite element spaces: $\mathbb{V}_{D,h} \times \mathbb{W}_{D,h} \subset H(\text{div}; \Omega) \times L^2(\Omega)$ for the Darcy part of the system
and $\mathbb{V}_{S,h} \times \mathbb{W}_{S,h} \subset (H^1(\Omega))^2 \times L^2(\Omega)$ for the Stokes part of the system.
We recall the aforementioned inf-sup stable mixed finite element space pairs on each element $E \in \mathcal{T}_h$:
\begin{itemize}
		  \item Reduced $H(\text{div})$ conforming space $\mathbb{V}_1^0$ in Section~\ref{sec:reducedHdiv},
\begin{equation}
\begin{aligned}
		  \mathbb{V}_{D,h}(E) = \mathbb{V}_1^0(E) &= (\mathbb{P}_1(E))^2 \oplus 
		  \text{curl}\,\mathbb{S}_2^{\mathcal{DS}}(E), \\
					 \mathbb{W}_{D,h}(E) = \mathbb{W}(E) &= \mathbb{P}_0(E);
\end{aligned}
\end{equation}

\item Modified Bernardi-Raugel element in Section~\ref{sec:mbr},
\end{itemize}
\begin{equation}
\begin{aligned}
		  \mathbb{V}_{S,h}(E) = \mathbb{V}^{\mathcal{BR}}_1(E) &= (\mathcal{DS}_1(E))^2 \oplus \text{span}\{\mathbf{b}_0,\mathbf{b}_1,\mathbf{b}_2,\mathbf{b}_3 \}, \\
		  \mathbb{W}_{S,h}(E) = \mathbb{W}(E)   &= \mathbb{P}_0(E).
		  \end{aligned}
\end{equation}
We impose essential boundary conditions by projecting extensions
$\tilde{\mathbf{g}}_r$ and $\tilde{\mathbf{g}}_s$ into
the finite element spaces as $\hat{\mathbf{g}}_r \in \mathbb{V}_{D,h}$ and $\hat{\mathbf{g}}_s \in \mathbb{V}_{S,h}$.

We also define 
\begin{equation}
		  \begin{aligned}
					 \mathbb{V}_{D,h,0} &= \{\mathbf{v} \in \mathbb{V}_{D,h} : \mathbf{v}\cdot \pmb{\nu} = 0 \hspace{0.2cm} \text{on} \hspace{0.2cm} \Gamma_{u,D}\} \\
					 \mathbb{V}_{S,h,\mathbf{0}} &= \{\mathbf{v} \in \mathbb{V}_{S,h} : \mathbf{v} = \mathbf{0} \hspace{0.2cm} \text{on} \hspace{0.2cm} \Gamma_{u,S}\} 
		  \end{aligned}
\end{equation}

\textit{The scaled mixed finite element method:} Find $\check{\mathbf{v}}_{r,h} \in \mathbb{V}_{D,h,0} + \hat{\mathbf{g}}_r$, $\check{q}_{r,h} \in \mathbb{W}_{D,h}$, 
$\check{\mathbf{v}}_{s,h} \in \mathbf{V}_{S,h,\mathbf{0}} + \hat{\mathbf{g}}_s$ and $\check{q}_h \in \mathbb{W}_{S,h}$ such that
\begin{alignat}{2}
		  \label{eq:weak_stokes}
		  &(\pmb{\tau}(\check{\bold{v}}_{s,h}),\nabla \pmb{\psi}_{s,h}) - (\check{q}_h, \nabla \cdot \pmb{\psi}_{s,h}) = -((1-\phi)\bold{n}, \pmb{\psi}_{s,h}), &&\forall\pmb{\psi}_{s,h} \in \mathbb{V}_{S,h,\mathbf{0}},\\
		  \label{eq:weak_compaction}
		  &(\nabla \cdot \check{\bold{v}}_{s,h}, \omega_h)  - \Big(\frac{\phi^{1/2}}{1-\phi} (\check{q}_{l,h} - \phi^{1/2}\check{q}_h), \omega_h \Big) = 0, &&\forall \omega_h \in \mathbb{W}_{S,h},\\
					\label{eq:weak_darcy}
					 &(\bold{\check{v}}_{r,h},\pmb{\psi}_{r,h})  - (\check{q}_{l,h},\phi^{-1/2} \nabla \cdot (\phi^{1+\Theta}\pmb{\psi}_{r,h})) = 0, &&\forall \pmb{\psi}_{r,h} \in \mathbb{V}_{D,h,0},\\
					 \nonumber
					 &(\phi^{-1/2} \nabla \cdot (\phi^{1+\Theta}\check{\bold{v}}_{r,h}),\omega_{l,h}) + \Big(\frac{1}{1-\phi}(\check{q}_{l,h} - \phi^{1/2}\check{q}_h),\omega_{l,h} \Big) &&= 0, \\
					 \label{eq:weak_continuous}
		  &					 &&\forall \omega_{l,h} \in 
		  \mathbb{W}_{D,h}.
\end{alignat}
The existence and convergence results of the proposed new mixed finite element discretization can be established
in complete analogy with the proof in~\cite{DegenerateDarcy}. We state the results below without repeating the full
derivation for completeness.
\begin{lem}
If~\eqref{eq:phiboundness} holds, then there exists a unique solution to the scaled mixed finite element 
method~\eqref{eq:weak_stokes}--\eqref{eq:weak_continuous}.
\end{lem}
\begin{proof}
See~\cite{DegenerateDarcy}.
\end{proof}
\begin{lem}
Assume that~\eqref{eq:phiboundness} holds on the porosity, $0\leq \phi \leq \phi^* <1$, the mesh
is quasiuniform. Then the difference of the solution to the scaled formulation~\eqref{eq:weak_stokes}--\eqref{eq:weak_continuous},
and its finite elment approximation satisfy
\begin{equation}
\begin{aligned}
&\norm{\tilde{\mathbf{v}}_r - \tilde{\mathbf{v}}_{r,h}}_{L^2(\Omega)} + 
\norm{\mathcal{P}_{\mathbb{W}_{D,h}}\big[\phi^{-1/2}\nabla \cdot(\phi^{1+\Theta}(\tilde{\mathbf{v}}_r - \tilde{\mathbf{v}}_{r,h})
)\big]}_{L^{2}(\Omega)} + \norm{\mathbf{v}_s - \mathbf{v}_{s,h}}_{H^1(\Omega)} \\
&+ \norm{\tilde{q}_l - \tilde{q}_{l,h}}_{L^2(\Omega)} + \norm{q - q_h}_{L^2(\Omega)} \\
&\leq C \bigg\{\norm{\tilde{\mathbf{v}}_r - \pi_{\mathbb{V}_1^0} \tilde{\mathbf{v}}_r}_{L^2(\Omega)}  + 
\norm{\mathcal{P}_{\mathbb{W}_{D.h}}[\phi^{-1/2}\nabla\cdot(\phi^{1+\Theta}(\tilde{\mathbf{v}}_r - \pi_{\mathbb{V}_1^0}\tilde{\mathbf{v}}_r))]}_{L^2(\Omega)}  \\
&+\norm{\pi_{\mathbb{V}_1^0}\tilde{\mathbf{g}}_r - \hat{\mathbf{g}}_r}_{L^2(\Omega)} + 
\norm{\tilde{q}_l - \mathcal{P}_{\mathbb{W}_{D,h}}\tilde{q}_l}_{L^2(\Omega)} + \norm{q - \mathcal{P}_{\mathbb{W}_{S,h}}q}_{L^2(\Omega)}\\ 
&+ \norm{\mathbf{v}_s - \pi_{\mathbb{V}_\mathcal{BR}}\mathbf{v}_s}_{H^1(\Omega)} + \norm{\pi_{\mathbb{V}_\mathcal{BR}}\tilde{\mathbf{g}}_s - \hat{\mathbf{g}}_s } \bigg\},
\end{aligned}
\end{equation}
where $\pi_{\mathbb{V}_{1}^0}$ and $\pi_{\mathbb{V}_\mathcal{BR}}$ are porjection operators defined in section~$\ref{sec:stabV10}$ 
and section~$\ref{sec:stabVbr}$ respectively.
\end{lem}
\begin{proof}
See~\cite{DegenerateDarcy}.
\end{proof}

\subsection{Local Mass Conservation}
Before proceeding, we ensure that the formulation is well-defined—specifically, 
that no division by zero occurs. 
The term $\phi^{-1/2}$ in the symmetric equations~\eqref{eq:weak_darcy} 
and~\eqref{eq:weak_continuous} causes trouble 
when there is no melting, i.e., $\phi = 0$.
To address this, we utilize an element-averaged variable $\phi_E$ as
\begin{equation}\label{eq:averaged_phi}
		  \phi_E = \frac{1}{\lvert E \rvert} \int_E \phi(\mathbf{x})\, d\mathbf{x},
\end{equation}
where $\lvert E \rvert$ is the area of element $E$ and 
$\phi_E = \mathcal{P}_{\mathbb{W}_{D,h}}\phi\rvert_E \in \mathbb{W}_{D,h}$. 
Then the two terms involving divergence is modified as
\begin{equation}\label{eq:newvariable_phi}
		  \phi^{-1/2}\nabla \cdot (\phi^{1+\Theta} \mathbf{v}) \rvert_E = \left\{
					 \begin{alignedat}{2}
								&\phi_E^{-1/2}\nabla \cdot (\phi^{1+\Theta} \mathbf{v})  \hspace{0.2cm} &&\text{if}\hspace{0.2cm} \phi_E \neq 0,\\
								&0 \hspace{0.5cm} &&\text{if} \hspace{0.2cm} \phi_E = 0,
					 \end{alignedat}
					 \right.
\end{equation}
where the point-wise term $\phi^{-1/2}$ is modified to be a cell-averaged term $\phi_E^{-1/2}$. 
We apply the divergence theorem to expand the integral term involving the
divergence operator in equation~\eqref{eq:weak_continuous} as
\begin{equation}\label{eq:divtheorem}
%		  \begin{aligned}
					 \int_E \phi_E^{-1/2} \nabla \cdot (\phi^{1+\Theta} \pmb{\psi}_{r,h})\omega_{l,h}\, d\mathbf{x} =\int_{\partial E} \phi_E^{-1/2} \phi^{1+\Theta} \pmb{\psi}_{r,h}\cdot \pmb{\nu} \omega_{l,h}\, ds, \\
%					 &+\int_{E}\phi_E^{-1/2}\phi^{1+\Theta}\pmb{\psi}_{r,h} \cdot \nabla \omega_{l,h}\, d\mathbf{x}.
%		  \end{aligned}
\end{equation}
where $\omega_{l,h} \in \mathbb{W}_{D,h}$ is constant on the element $E \in \mathcal{T}_h$.
%The second term vanishes when $\omega_{l,h}$ is constant on element $E$.
We now propose our local mass conserved version %by
%replacing the term $\phi^{1/2}$ in equation~\eqref{eq:weak_compaction} 
with $\phi_E^{1/2}$ as
\iffalse
\begin{alignat}{2}
		  \label{eq:weak_stokes_mass}
		  &(\check{\pmb{\tau}}(\bold{v}_{s,h}),\nabla \pmb{\psi}_s) - (\check{q}_h, \nabla \cdot \pmb{\psi}_s) = -((1-\phi)\bold{n}, \pmb{\psi}_s) &&\forall\pmb{\psi}_s \in \mathbb{V}_1^{\mathcal{BR}},\\
		  \label{eq:weak_compaction_mass}
		  &(\nabla \cdot \check{\bold{v}}_{s,h}, \omega)  - \Big(\frac{\phi_E^{1/2}}{1-\phi} (\check{q}_{l,h} - \phi^{1/2}\check{q}_h), \omega\Big) = 0 &&\forall \omega \in \mathbb{W}_0^{\mathcal{BR}},\\
					\label{eq:weak_darcy_mass}
					 &(\bold{\check{v}}_{r,h},\pmb{\psi}_r)  - (\check{q}_{l,h},\phi_E^{-1/2} \nabla \cdot (\phi^{1+\Theta}\pmb{\psi}_{r,h})) = 0, &&\forall \pmb{\psi}_r \in \mathbb{V}_1^0,\\
					 \nonumber
					 &(\phi_E^{-1/2} \nabla \cdot (\phi^{1+\Theta}\check{\bold{v}}_{r,h}),\omega_f) + \Big(\frac{1}{1-\phi}(\check{q}_{l,h} - \phi^{1/2}\check{q}_h),\omega_f \Big) &&= 0 \\
					 \label{eq:weak_continuous_mass}
		  &					 &&\forall \omega_f \in 
		  \mathbb{W}_{0}.
\end{alignat}
\fi
\begin{alignat}{2}
		  \label{eq:weak_stokes_mass}
		  &(\pmb{\tau}(\check{\bold{v}}_{s,h}),\nabla \pmb{\psi}_{s,h}) - (\check{q}_h, \nabla \cdot \pmb{\psi}_{s,h}) = -((1-\phi)\bold{n}, \pmb{\psi}_{s,h}) &&\forall\pmb{\psi}_{s,h}, \in \mathbb{V}_{S,h,\mathbf{0}},\\
		  \label{eq:weak_compaction_mass}
		  &(\nabla \cdot \check{\bold{v}}_{s,h}, \omega_h)  - \Big(\frac{\phi\phi_E^{-1/2}}{1-\phi} (\check{q}_{l,h} - \phi_E^{1/2}\check{q}_h), \omega_h \Big) = 0, &&\forall \omega_h \in \mathbb{W}_{S,h},\\
					\label{eq:weak_darcy_mass}
					 &(\bold{\check{v}}_{r,h},\pmb{\psi}_{r,h})  - (\check{q}_{l,h},\phi_E^{-1/2} \nabla \cdot (\phi^{1+\Theta}\pmb{\psi}_{r,h})) = 0, &&\forall \pmb{\psi}_{r,h} \in \mathbb{V}_{D,h,0},\\
					 \nonumber
					 &(\phi_E^{-1/2} \nabla \cdot (\phi^{1+\Theta}\check{\bold{v}}_{r,h}),\omega_{l,h}) + \Big(\frac{\phi\phi^{-1}_E}{1-\phi}(\check{q}_{l,h} - \phi_E^{1/2}\check{q}_h),\omega_{l,h} \Big) &&= 0, \\
					 \label{eq:weak_continuous_mass}
		  &					 &&\forall \omega_{l,h} \in 
		  \mathbb{W}_{D,h}.
\end{alignat}
Here, the term $\phi \phi_E^{-1}$ in equation~\eqref{eq:weak_continuous_mass} is viewed as 1 when $\phi_E = 0$.
To verify local mass conservation of the fluid, consider any element $E \in \mathcal{T}_h$, 
equation~\eqref{eq:weak_continuous_mass} gives
\begin{equation}
		  \int_E \nabla \cdot (\phi^{1+\Theta} \check{\mathbf{v}}_{r,h})\, d\mathbf{x} + \int_E \frac{\phi}{1-\phi} (\check{q}_{l,h} - \phi_E^{1/2}\check{q}_h)\, dx = 0,
\end{equation}
and equation~\eqref{eq:weak_compaction_mass} gives
\begin{equation}
		  \int_E \nabla \cdot \mathbf{v}_{s,h}\, d\mathbf{x} - \int_E \frac{\phi}{1-\phi} (\check{q}_{l,h} - \phi_E^{1/2}\check{q}_h)\, dx = 0.
\end{equation}
%We define an auxiliary space $\omega_E$ being 1 on $E$ and 0 elsewhere.
We sum equations~\eqref{eq:weak_compaction_mass} and~\eqref{eq:weak_continuous_mass} 
together 
assuming that $\omega_h = \omega_E \in \mathbb{W}_{S,h}$ and $\omega_{l,h} = \phi_E^{-1/2} \omega_E \in \mathbb{W}_{D,h}$. 
The local mass conservation of the phase-averaged mixture is now retrieved as
\begin{equation}
		  \int_{E}\nabla \cdot (\phi^{1+\Theta}\check{\mathbf{v}}_r + \check{\mathbf{v}}_s)\, d\mathbf{x} = 0.
\end{equation}

\begin{lem}
		  Assuming~\eqref{eq:phiboundness} holds, there exists a unique solution
		  to the modified local mass conservation modified
		  scaled mixed finite element method
		  ~\eqref{eq:weak_stokes_mass}--\eqref{eq:weak_continuous_mass}.
\end{lem}
\begin{proof}
		  See~\cite{DegenerateDarcy}.
		  The local mass conservation modification 
		  replace the term $1/(1-\phi)$ with $(\phi \phi_E^{-1})/(1-\phi)$
		  ,which makes it completely analogous to the previous
		  proof. Hence, we are not repeating here.
\end{proof}

We now derive a bound for the local mass conservation modified scaled mixed
finite element method.

We derive a bound for the error by taking the difference
of 

We use the projection operators defined in 

\subsection{Recovery of the unscaled Darcy variables}
The unscaled Darcy potential is $\check{q}_f = \phi^{-1/2}\tilde{\check{q}}_l$, see~\eqref{eq:scaledVariables}. 
We can reterive an approximate Darcy potential. We assume that $\check{q}_{f,h} \in \mathbb{W}_{D,h}$, so on
an element $E$ we define
\begin{equation}
		  \check{q}_{l,h}\rvert_{E} = \left\{
					 \begin{alignedat}{2}
								&\phi_E^{-1/2} \tilde{\check{q}}_{l,h}\lvert_E \quad &&\text{if}\, \phi_E \neq 0,\\
								&0                                                   &&\text{if}\, \phi_E = 0,
					 \end{alignedat}
					 \right.
\end{equation}
where the liquid pressure potential $\check{q}_l$ does not hold any physical 
meanings when there is no melting presents ($\phi = 0$).

The relative Darcy velocity is given by $\check{\mathbf{v}}_r = \phi^{\Theta} \tilde{\check{\mathbf{v}}}$, 
see~\eqref{eq:scaledVariables} as well. To maintain local mass balance, we 
enforce that on each edge $e_i \subset \partial E$
\begin{equation}
		  \int_{e_i} \phi^{1+\Theta} \tilde{\check{\mathbf{v}}}_r \cdot \pmb{\nu}_i\, ds = \int_{e_i} \phi \check{\mathbf{v}}_{r,h} \cdot \pmb{\nu} \, ds.
\end{equation}

\input chapters/linear.tex

\section{Convergence Test}
We test our MFEM setup with two different scenarios.
First, we test a pseudo 1D problem but with 2D elements equipped with three different
porosity scenarios.
Second, we test a
more complex yet a more realistic problem representing Mid Ocean Ridge where
no true solution is presented.

\iffalse
Problem Setup 1: Manufactured Solution.

We solve a manufactured system of equations on the rectangular domain 
$(x,y) \in [-1,1]^2$. We assume a constant porosity acorss the domain as
$\phi$. We compute the right hand side with prescribed functions as

\begin{equation}\label{eq:test1}
\check{q} = 0 \quad \quad \check{q}_f = 2(x+y) \quad \quad 
\check{\bold{v}}_r = -\frac{\phi_f^{-1/2}}{1-\phi_f}\begin{pmatrix}x^2 \\ y^2 \end{pmatrix} 
\quad \quad \check{\bold{v}}_s = \frac{\phi_f^{1/2}}{1-\phi_f} \begin{pmatrix}x^2 \\ y^2 \end{pmatrix}
\end{equation}
\begin{equation}
\nabla \cdot \check{\bold{v}}_r = \frac{-2\phi_f^{-1/2}}{1-\phi_f}(x+y) 
\end{equation}
\begin{equation}
\mathcal{D}\check{\bold{v}}_s = \frac{2\phi_f^{1/2}}{1-\phi_f}\begin{bmatrix} x & 0 \\ 0 & y \end{bmatrix}
\quad \quad 
(\nabla \cdot \check{\bold{v}}_s) \bold{\text{I}} = \frac{2\phi_f^{1/2}}{1-\phi_f} \begin{bmatrix} x+y & 0 \\ 0 & x+y \end{bmatrix}
\end{equation}
Substitute the compuated results into PDE
\begin{equation}
\begin{aligned}
\tilde{\bold{v}}_r + \phi_f^{1/2}\nabla\check{q}_f &= 
\frac{\phi_f^{-1/2}}{1-\phi_f}\begin{pmatrix}x^2 \\ y^2 \end{pmatrix} + \phi_f^{1/2}\begin{pmatrix} 2 \\ 2 \end{pmatrix}\\
\phi_f^{1/2}\nabla \cdot (\check{\bold{v}}_r) + \frac{1}{1-\phi_f}\check{q}_f  &= \frac{-2}{1-\phi_f}(x+y) + \frac{1}{1-\phi_f}2(x+y)=0\\
\nabla \cdot \check{\bold{v}}_s - \frac{\phi_f^{1/2}}{1-\phi_f}\check{q}_f &= \frac{2\phi_f^{1/2}}{1-\phi_f}(x+y) - \frac{2\phi_f^{1/2}}{1-\phi_f}(x+y) = 0\\
-2(1-\phi_f)\nabla \cdot \sigma(\check{\bold{v}}_s) &= -2(1-\phi_f)\nabla \cdot (\mathcal{D}\check{\bold{v}}_s - \frac{1}{3}(\nabla \cdot \check{\bold{v}}_s) \bold{\text{I}}) = 
-\frac{8\phi_f^{1/2}}{3} \begin{pmatrix} 1 \\ 1 \end{pmatrix}
\end{aligned}
\end{equation}
\fi

In this subsection, we perform a convergence test on a 2D grid with a pseudo 1D problem.
We study a compacting column in one direction. The column extends over $y \in [-L,L]$
and has no flow through top and bottom boundaries. We prescribe the following boundary conditions
\begin{itemize}
		  \item Bottom Edge;
					 \begin{itemize}
								\item Essential boundary condition:
										  \begin{equation*}
										  \left\{
													 \begin{aligned}
																 \check{\mathbf{v}}_s &= \mathbf{0};\\
																\mathbf{b} &= \mathbf{0};
													 \end{aligned}\right.
										  \end{equation*}
								\item Essential boundary condition: Normal Darcy flux $\check{\mathbf{v}}_r\cdot \pmb{\nu}= 0$;
					 \end{itemize}
		  \item Left and Right side Edges;
					 \begin{itemize}
								\item y component of Stokes velocity is free, x component is kept as zero;
								\item Darcy velocity, which is understood as flux is also kept zero;
							   \item normal flux dof fixed with a zero flux.
					 \end{itemize}
		  \item Top Edge;
					 \begin{itemize}
								\item Stokes velocity $\check{\mathbf{v}}_s = \mathbf{0}$;
								\item Darcy velocity $\check{\mathbf{v}}_l = \mathbf{0}$;
							   \item Normal flux is also determined to be zero.
					 \end{itemize}
\end{itemize}
We assume no velocity in x direction, $\check{\mathbf{v}}_s \cdot \mathbf{i} = 0$, 
and $\check{\mathbf{v}}_r \cdot \mathbf{i} = 0$, where $\mathbf{i}$ is 
the unit vector in x direction pointing from left to right.
Let $u = \phi \check{\mathbf{v}}_r \cdot \mathbf{j}$
and $v = \check{\mathbf{v}}_s \cdot \mathbf{j}$, where $\mathbf{j}$
is the unit vector in y direction pointing upwards.
\begin{equation}\label{eq:qdcolumncompaction}
		  \begin{aligned}
					 u + \phi^{2} q'_l &= 0, \\
					 u' + \phi(q_l - q_s)&= 0, \\
					 [q_s - \frac{1}{3}(1-4\phi)v']' &= 1-\phi,\\
					 v' - \phi(q_f - q_s) &=0,
		  \end{aligned}
\end{equation}
When no melting presents $\phi = 0$, the equations reduce to 
\begin{equation}
		  u = -v = 0,\quad\text{and}\quad q'_s = 1, \quad\text{and}\quad q_l = 0.
\end{equation}
When melting presents $\phi > 0$, the equations can be reduced to
\begin{equation}\label{eq:ode}
		  \phi^{2+2\Theta}\bigg(\frac{3+\phi - 4\phi^2}{3\phi}u' \bigg)' - u = \phi^{2+2\Theta}(1-\phi),
\end{equation}
where all other quantities can be determined according to $u$ as
\begin{equation}
		  v=-u, \quad \text{and}\quad  q'_l = -\phi^{-2-2\Theta}u, \quad \text{and}\quad q_s = u'/\phi + q_l.
\end{equation}
When $\phi$ is constant, we define an auxiliary function $R$ as
\begin{equation}
		  R = \bigg( \phi^{2+2\Theta}\frac{3+\phi - 4\phi^2}{3\phi} \bigg)^{-1/2}.
\end{equation}

We test our MFEM code on three porosity settings,
and use the closed form solutions derived in~\cite{DegenerateDarcy}.
\subsection{Column compaction with Constant porosity}
					 \begin{equation}
								\phi_0(z) = \phi_0;
					 \end{equation}
					 Closed form solution:
					 \begin{equation}
								\begin{aligned}
										  u &= -\phi_0^2 (1-\phi_0) \bigg[1 - \frac{\cosh{(Rz)}}{\cosh{(RL)}} \bigg], \\
                                v &= -u, \\
										  q_l &= (1-\phi_0) \bigg[z - \frac{1}{R}\frac{\sinh{(Rz)}}{\cosh{(RL)}} \bigg] + c,\\
										  q_s &= (1-\phi_0) \bigg[z - \frac{1-4\phi_0}{3+\phi_0-4\phi_0^2} \frac{\phi_0}{R}\frac{\sinh{(Rz)}}{\cosh{(RL)}} \bigg] + c,
								\end{aligned}
					 \end{equation}
where $c$ is a constant kernel.
					 \begin{figure}[ht]
						\centering
								\includegraphics[width=3.5cm,height=9cm]{chapters/pics/porosity_const.eps}
\includegraphics[width=3.5cm,height=9cm]{chapters/pics/vel_const.eps}
\includegraphics[width=3.5cm,height=9cm]{chapters/pics/vel_quiver_const.eps}
								\includegraphics[width=3.5cm,height=9cm]{chapters/pics/q_const.eps}
								\caption{Constant porosity compaction column test.}
								\label{fig:constporocompactcolumn}
					 \end{figure}

					 \begin{table}[ht]
\centering
					\begin{tabular}{c || c | c || c | c || c | c || c | c }
							  \quad & \multicolumn{2}{c||}{$u$} & \multicolumn{2}{c||}{$v$}& \multicolumn{2}{c||}{$q_l$} & \multicolumn{2}{c}{$q_s$} \\
							  $m\times n$ & $L^2$ error & rate & $L^2$ error & rate & $L^2$ error & rate & $L^2$ error & rate \\
							  \hline
							  $2\times20$ & 3.100e-05 & - & 3.100e-05 & - & 3.285e-03 & - & 3.638e-05 & -\\
							  $2\times40$ & 8.047e-06 & 1.95 & 8.047e-06 & 1.95 & 8.731e-04 & 1.91 & 9.671e-06 & 1.91\\
							  $2\times80$ & 2.031e-06 & 1.99 & 2.031e-06 & 1.99 & 2.218e-04 & 1.98 & 2.457e-06 & 1.98\\
							  $2\times160$ & 5.091e-07 & 2.00 & 5.091e-07 & 2.00 & 5.567e-05 & 1.99 & 6.166e-07 & 1.99
					\end{tabular}
								\caption{Constant porosity compaction column test.}
								\label{tab:constporositycolumn}
					 \end{table}

\subsection{Piecewise constant porosity}
					 \begin{equation}
								\phi_J(z) = \left \{
										  \begin{aligned}
													 &0 \hspace{0.6cm} \text{if}\hspace{0.2cm}  z> 0,\\
													 &\phi_0 \hspace{0.4cm} \text{if}\hspace{0.2cm} z\leq 0,
										  \end{aligned}
										  \right.
					 \end{equation}
					 Closed form solution:

					 We conclude that $u=-v=0$ when $z>0$, and $q_l = 0$, $q_s = z + c$.
					 When $z<0$, $\phi_0 \neq 0$,
					 \begin{equation}
								\begin{aligned}
										  u &= \phi_0^2(1-\phi_0) \bigg[1 - \cosh(Rz) + \frac{1 - \cosh{(RL)}}{\sinh{(RL)}}\sinh{(Rz)} \bigg],\\
								 v &= -u, \\
										  q_l &= -(1-\phi_0)\bigg[z - \frac{1}{R}\sinh(Rz) + \frac{1-\cosh(RL)}{R\sinh(RL)}\cosh(Rz)\bigg] + c,\\
										  q_s &= q_l + \frac{u'}{\phi_0} + c,
								\end{aligned}
					 \end{equation}
                where $c$ is a constant kernel shifting the pressure potential.

					 \begin{figure}[ht]
						\centering
								\includegraphics[width=3.5cm,height=9cm]{chapters/pics/porosity_piececonst.eps}
\includegraphics[width=3.5cm,height=9cm]{chapters/pics/vel_piececonst.eps}
\includegraphics[width=3.5cm,height=9cm]{chapters/pics/vel_quiver_piececonst.eps}
\includegraphics[width=3.5cm,height=9cm]{chapters/pics/q_piececonst.eps}
								\caption{Piecewise constant porosity compaction column test.}
								\label{fig:piececonstporocompactcolumn}
					 \end{figure}

					 \begin{table}
					\begin{tabular}{c || c | c || c | c || c | c || c | c }
							  \quad & \multicolumn{2}{c||}{$u$} & \multicolumn{2}{c||}{$v$}& \multicolumn{2}{c||}{$q_l$} & \multicolumn{2}{c}{$q_s$} \\
							  $m\times n$ & $L^2$ error & rate & $L^2$ error & rate & $L^2$ error & rate & $L^2$ error & rate \\
							  \hline
							  $2\times20$ & 2.980e-05 & - & 2.980e-05 & - & 3.794e-03 & - & 4.202e-05 & -\\
							  $2\times40$ & 7.681e-06 & 1.96 & 7.681e-06 & 1.96 & 1.005e-03 & 1.92 & 1.114e-05 & 1.92\\
							  $2\times80$ & 1.958e-06 & 1.97 & 1.958e-06 & 1.97 & 2.472e-04 & 2.02 & 2.738e-06 & 2.02\\
							  $2\times160$ & 4.974e-07 & 1.98& 4.974e-07 & 1.98 & 5.972e-05 & 2.04 & 6.615e-07 & 2.05 
					\end{tabular}
								\caption{Piecewise Constant porosity compaction column test.}
								\label{tab:constporositycolumn}
					 \end{table}

\subsection{Column compaction with quadratic porosity.}
					 \begin{equation}
								\phi_2(z) = \left \{
										  \begin{aligned}
													 &0 \hspace{0.95cm} \text{if}\hspace{0.2cm}  z> 0,\\
													 &\phi_0z^2 \hspace{0.3cm} \text{if}\hspace{0.2cm} z\leq 0.
										  \end{aligned}
										  \right.
					 \end{equation}
%					 Closed form solution:
%					 \begin{equation}
%								\begin{aligned}
%										  u &= \frac{\phi_0^2}{1-4\phi_0}\bigg(L^{4-r_1}z^{r_1} - z^4\bigg),\\
%										  v &= -v\\
%										  q_l&= \frac{1}{1-4\phi_0} \bigg(z + \frac{L^{4-r_1}(-z)^{r_1-3}}{r_1-3} \bigg) + c,\\
%										  q_s&= z + c,
%								\end{aligned}
%					 \end{equation}
%					 where 
%					 \begin{equation}
%								r_1 = \frac{3+\sqrt{9+4/\phi_0}}{2}.
%					 \end{equation}
We are interested in presenting the result when $\phi_0 = 0.01$, which is not approximated precisely using previous
closed form solution due to the assumption $\phi \ll 1$ does not hold anymore.
Here, we present an alternative way of constructing approximation solution of the
second order ordinary differential equation~\eqref{eq:ode}. 
Let $v = u/\phi^2 = u/(\phi_0^2 z^4)$,
\begin{equation}
		  v' = \frac{-4v}{z} + \frac{u'}{\phi_0^2 z^4}.
\end{equation}
Substitute $u$ with $v$, we have 
\begin{equation}\label{eq:nonhomoode}
		  f(u'',u',u,z) = \bigg[ \frac{3+\phi_0z^2 - 4\phi_+z^4}{3\phi_0z^2}\big(\phi_0^2 z^4 v' + 4\phi_0^2 z^3 v\big) \bigg]' - v = 1 - \phi_0z^2.
\end{equation}
We assume that there exists a solution that can be represented as a sum of 
an infinity series.
The solution can be understood as the sum of a particular
solution and a homogeneous power-series solution,
which solves the homogeneous second order ODE $f(u'',u',u',z) = 0$.
We apply Frobenius method here, assuming the homogeneous solution
has the form as
\begin{equation}\label{eq:homogenous}
v_H = (z-z_0)^r \sum_{n=0}^\infty a_n (z-z_0)^n,
\end{equation}
where we pick $z_0 = 0$ for simplicity. We write the homogeneous
recurrence as
\begin{equation}\label{eq:recursive}
\begin{aligned}
&\bigg\{ \phi_0 \Big[(n+r-1)(n+r) + 6(n+r) + 4 \Big] - 1 \bigg\} a_n + \\ 
&\bigg\{ \frac{\phi_0^2}{3} \Big[(n+r-2)(n+r+3) + 4 + 2(n+r+2) \Big] \bigg\} a_{n-2} +\\
&\bigg\{ \frac{4\phi_0^3}{3} \Big[ (n+r-4)(n+r+1) +4 +4(n+r) \Big] \bigg\} a_{n-4}  = 0,
\end{aligned}
\end{equation}
where we obtain the indicial equation be equating the coefficient associated to $a_0$ term to zero, i.e.,
\begin{equation}
\phi_0\big[(0+r-1)(0+r) +6(0+r) + 4\big] - 1 = 0,
\end{equation}
where we solve for the positive root as
\begin{equation}
r = \frac{-5+\sqrt{9+4/\phi_0}}{2}.
\end{equation}
Notice that odd and even coefficients are independent against each other, we can decompose the 
homogeneous solution into two independent power-series according to even and odd coefficients as
\begin{equation}
v_1 = z^r \sum_{n=0}^{\infty} a_{2n} z^{2n}, \quad v_2 = z^r \sum_{n=0}^{\infty} a_{2n+1} z^{2n+1}.
\end{equation}
Then we proceed to determine the particular solution that solves the
non-homogeneous function $f(u'',u',u,z) = 1-\phi_0z^2$.
\begin{equation}
v_p = \sum_{n=0}^\infty c_n z^n.
\end{equation}
Substituting the particular solution into the non-homogeneous ODE~\eqref{eq:nonhomoode}, we obtain the coefficient 
by matching the coefficients on the right hand side according to different orders.
\begin{equation}
\begin{aligned}
c_0 & = \frac{1}{4\phi_0 - 1} \\
c_2 & = \frac{-\phi_0 - 4\phi_0^2 c_0}{18\phi_0 - 1} \\
c_4 & = \frac{80/3 \phi_0^3 c_0  - 10 \phi_0^2 c_2}{40\phi_0 -1}.
\end{aligned}
\end{equation}
The rest of the coefficients can be determined with the recurrence form~\eqref{eq:recursive}.
Omitting the power-series $v_2$ containing only odd coefficient, we have the final form of
the series solution as
\begin{equation}
v = v_p + C v_1,
\end{equation}
and we determine the costant $C$ with boundary condition $v(-2) = 0$, i.e., $C = -v_p(-2)/v_1(-2)$.

					 \begin{figure}[ht]
						\centering
								\includegraphics[width=3.5cm,height=9cm]{chapters/pics/porosity_quad.eps}
\includegraphics[width=3.5cm,height=9cm]{chapters/pics/vel_quad.eps}
\includegraphics[width=3.5cm,height=9cm]{chapters/pics/vel_quiver_quad.eps}
								\includegraphics[width=3.5cm,height=9cm]{chapters/pics/q_quad.eps}
								\caption{Quadratic porosity compaction column test $\phi_0 = 0.001$.}
								\label{fig:quadraticporocompactcolumn}
					 \end{figure}
\iffalse
					 \begin{table}
					\begin{tabular}{c || c | c || c | c || c | c || c | c }
							  \quad & \multicolumn{2}{c||}{$u$} & \multicolumn{2}{c||}{$v$}& \multicolumn{2}{c||}{$q_l$} & \multicolumn{2}{c}{$q_s$} \\
							  $m\times n$ & $L^2$ error & rate & $L^2$ error & rate & $L^2$ error & rate & $L^2$ error & rate \\
							  \hline
							  $2\times20$ &  9.616e-07 & -& 9.616e-07 & - & - & - & - & -\\
							  $2\times40$ &  3.212e-07 & 1.58 & 3.212e-07 & 1.58 & - & - & - & -\\
							  $2\times80$ &  9.028e-08 & 1.83 & 9.028e-08 & 1.83 & - & - & - & -\\
							  $2\times160$ & 2.746e-08 & 1.72 & 2.746e-08 & 1.72 & - & - & - & -
					\end{tabular}
								\caption{Quadratic porosity compaction column test $\phi_0 = 0.001$.}
								\label{tab:constporositycolumn}
					 \end{table}
					 \fi
Now we redo the convergence test $\phi = \phi_0 z^2$
					 \begin{table}
					\begin{tabular}{c || c | c || c | c || c | c || c | c }
							  \quad & \multicolumn{2}{c||}{$u$} & \multicolumn{2}{c||}{$v$}& \multicolumn{2}{c||}{$q_l$} & \multicolumn{2}{c}{$q_s$} \\
							  $m\times n$ & $L^2$ error & rate & $L^2$ error & rate & $L^2$ error & rate & $L^2$ error & rate \\
							  \hline
							  $2\times20$ &  9.558e-07 & -& 9.558e-07 & - & 3.088e-03 & - & 4.660e-06 & -\\
							  $2\times40$ &  3.176e-07 & 1.59 & 3.176e-07 & 1.59 & 1.525e-03 & 1.02 & 2.196e-06 & 1.09\\
							  $2\times80$ &  8.686e-08 & 1.87 & 8.686e-08 & 1.87 & 7.442e-04 & 1.04 & 1.130e-06 & 0.96\\
							  $2\times160$ & 2.222e-08 & 1.97 & 2.222e-08 & 1.97 & 2.893e-04 & 1.36 & 5.578e-07 & 1.02
					\end{tabular}
								\caption{Quadratic porosity compaction column test $\phi_0 = 0.001$.}
								\label{tab:constporositycolumn}
					 \end{table}
Since we do not make the assumetion that $\phi_0 z^ \ll 1$ anymore, we test convergence on the case where $\phi_0 = 0.1$.
\iffalse
					 \begin{figure}[ht]
						\centering
						\begin{minipage}[t]{0.47\textwidth}
						\centering
								\includegraphics[width=3cm,height=8cm]{chapters/pics/vel_quad_0.01_euler.eps}
								\subcaption{True solution with previous form.}
								\end{minipage}
								\begin{minipage}[t]{0.47\textwidth}
								\centering
\includegraphics[width=3cm,height=8cm]{chapters/pics/vel_quad_0.01_series.eps}
\subcaption{Ture solution in series form.}
\end{minipage}
								\caption{Comparison two closed form solution to quadratic porosity compaction column test $\phi_0 = 0.01$, 
								$N = 160$.}
								\label{fig:quadraticporocompactcolumn}
					 \end{figure}
\fi
					 \begin{figure}[ht]
						\centering
								\includegraphics[width=3.5cm,height=9cm]{chapters/pics/porosity_quad_0.1.eps}
\includegraphics[width=3.5cm,height=9cm]{chapters/pics/vel_quad_0.01_series.eps}
\includegraphics[width=3.5cm,height=9cm]{chapters/pics/vel_quiver_quad_0.01_series.eps}
								\includegraphics[width=3.5cm,height=9cm]{chapters/pics/q_quad_0.1.eps}
								\caption{Quadratic porosity compaction column test $\phi_0=0.01$.}
								\label{fig:quadraticporocompactcolumn_0.01}
					 \end{figure}

					 \begin{table}
					\begin{tabular}{c || c | c || c | c || c | c || c | c }
							  \quad & \multicolumn{2}{c||}{$u$} & \multicolumn{2}{c||}{$v$}& \multicolumn{2}{c||}{$q_l$} & \multicolumn{2}{c}{$q_s$} \\
							  $m\times n$ & $L^2$ error & rate & $L^2$ error & rate & $L^2$ error & rate & $L^2$ error & rate \\
							  \hline
							  $2\times20$ &  2.438e-05 & -& 2.438e-05 & - &  & - &  & -\\
							  $2\times40$ &  6.249e-06 & 1.96 & 6.249e-06 & 1.96 &  &  &  & \\
							  $2\times80$ &  1.486e-06 & 2.07 & 1.486e-06 & 2.07 &  &  &  & \\
							  $2\times160$ & 3.908e-07 & 1.96 & 3.908e-07 & 1.96 &  &  &  & 
					\end{tabular}
								\caption{Quadratic porosity compaction column test $\phi_0 = 0.01$.}
								\label{tab:constporositycolumn}
					 \end{table}


\section{Mid-Ocean Ridge Test}

We now reconsider the Mid-Ocean ridge test case in~\cite{DegenerateDarcy}.
We solve the full system of equations on a dimensionless 
rectangular domain $x \in [-0.5,0.5]$ and $z \in[-0.5, 0.0]$.
The boundary conditions are imposed by
assigning classical corner flow solution of
Stokes velocity in~\cite{SPIEGELMAN1987137}
as essential boundary condition all around
the computational domain 
\begin{equation}\label{eq:test2}
		  \mathbf{v}_s = \frac{2U_0}{\pi(x^2+y^2)}\begin{pmatrix}
					 \tan^{-1}(\frac{x}{-y})(x^2+y^2) + xy\\
z^2
		  \end{pmatrix},
\end{equation}
where $U_0 = 10^{-9}\text{m/s} \approx 3.2\text{cm/yr}$ is the 
constant upwelling velocity of mantle.
According to mass conservation condition, we set no Darcy flux across the 
boundary.
We then set a not identically vanishing porosity by the formula
\begin{equation}\label{eq:porositydistri}
		  \phi(x,z) = \left \{ \begin{aligned}
					 &0.05 (\frac{0.375 + y}{0.375})^2 (1 - \frac{\lvert x \rvert}{\lvert y \rvert + l}), \hspace{0.2cm} \text{if $\lvert y \rvert \leq 0.375$ and $\lvert x \rvert \leq z + l$},\\
					 &0, \hspace{5.2cm}\text{otherwise},
		  \end{aligned}
		  \right.
\end{equation}
where $l = 20\,\text{m}$ is the offset set to avoid singularity computation
at the center $x=0$ we translate the coordinates $x$ to $x-l$ if $x<0$, and 
$x+l$ when $x>0$.
In Figure~\ref{fig:MORporosity}, we show the porosity distribution in the
computational domain.
Unlike the previous test conducted in the quarter plane 
where $x\in (0,0.5],\, y \in [-0.5,0.0]$.
We perform the numerical test within the entire plane instead. 
\begin{figure}[ht]
		  \centering
\includegraphics[width=16cm,height=8cm]{chapters/pics/mor_poro.eps}
		  \caption{Porosity distribution.}
		  \label{fig:MORporosity}
\end{figure}

\begin{figure}[ht]
		  \centering
\includegraphics[width=16cm,height=8cm]{chapters/pics/mor_l.eps}
		  \caption{Liquid pressure potential and velocity.}
		  \label{fig:MORliquid}
\end{figure}
\begin{figure}[ht]
		  \centering
\includegraphics[width=16cm,height=8cm]{chapters/pics/mor_s.eps}
		  \caption{Solid pressure potential and velocity.}
		  \label{fig:MORsolid}
\end{figure}
In Figure~\ref{fig:MORliquid} and Figure~\ref{fig:MORsolid}, we
exhibit the nondimensional Stokes velocity $(\check{\mathbf{v}}_s,\check{q}_s)$ 
and Darcy velocity $(\phi \check{\mathbf{v}}_r, \check{q}_l)$ respectively.
We observe the molten magma rise to the surface and focus to the region where
most porosity presents. The solid matrix upwell from the bottom of the domain. 
We observe compaction at degree of melting (some porosity) where the buoyancy
can no longer support the solid matrix on top of it due to density difference
between solid and liquid.
Despite of the presence of large no melting region, 
the system is solved successfully with modified Uzawa iteration
