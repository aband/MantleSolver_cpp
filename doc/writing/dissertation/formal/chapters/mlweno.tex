\definecolor{myBlue}{RGB}{190,203,211}
\definecolor{myRed}{RGB}{244,186,175}
\definecolor{myGreen}{RGB}{114,138,122}
\definecolor{myYellow}{RGB}{241,208,157}

\section{The Multi-level Weighted Essentially Non-Oscillatory (ML-WENO) Method}
We develop a accurate, versatile, robust and efficient finite volume 
weighted essentially non oscillatory (WENO) techniques for solving energy and composition 
conservation laws in~\cite{MultiLevelWeno}. 

\subsection{Stencil Polynomial}
Given a stencil $S = \{E_0, E_2, \dots, E_k\}$ of the quasiuniform logically rectangular mesh. We can define
an associated tensor product stencil polynomial 
$Q_{s,t} \in \mathbb{Q}_{(s,t)} \vcentcolon= \mathbb{P}_{s-1}(x) \otimes \mathbb{P}_{t-1}(y)$,
where $s$ and $t$ equal to the stencil sizes in the 
x- and y-directions, respectively.
We scale our stencil polynomial individually as
\begin{equation} \label{eq:stencilpolynomial}
		  Q_{s,t} = \sum_{p<s, q<t} c_{p,q} \Big(\frac{x - x_S}{h_S}\Big)^p 
		  \Big(\frac{y - y_S}{h_S}\Big)^q,
\end{equation}
where $(x_S,y_S)$ is the center point of the stencil 
$\Omega_S = \cup_{E\in S} \subset \mathbb{R}^2$, and $h_S = 
\sqrt{\sum_{k}E_k/k}$ is the diameter of 
average cell size.
In finite volume scenerio, we are dealing with cell averaged value of the
scalar transport variable
\begin{equation}\label{eq:cellaveraged}
		  \overline{u}_E = \frac{1}{|E|}\int_E u(\mathbf{x}) d\mathbf{x} = \frac{1}{|E|} \int_{E}u(x,y) dx dy.
\end{equation}
We can calculate coefficents $c_{p,q}$ by imposing a constraint that cell averaged
value \eqref{eq:cellaveraged} is preserved through integral with stencil polynomial
\begin{equation}\label{eq:stencilpolycoefficent}
		  \frac{1}{|E_k|} \int_{E_k} Q(\mathbf{x}) d\mathbf{x} = \overline{u}_{E_k}, \quad 
		  \forall E_k \in S.
\end{equation}
I will replace $\overline{u}_{E_k}$ with $\overline{u}_k$ in the following
discussion for simplicity.

We can represent stencil polynomial with basis polynomials 
$Q_{s,t} = \sum_{k} \overline{u}_{k} \phi^k_{s,t}$,
where basis polynomials can be calcualted as
\begin{equation}\label{eq:basispoly}
		  \frac{1}{|E_{k'}|} \int_{E_{k'}} \phi^k_{s,t} (\mathbf{x}) d\mathbf{x} = \left \{
					 \begin{aligned}
								&1 \quad  k' = k\\
								&0 \quad  k' \neq k
					 \end{aligned}
					 \right. \quad \forall (E_{k'}, E_k) \in S,
\end{equation}
which would lead to a $(s \times t) \times (s \times t)$ non-singular linear system. 
We can define a linear projection operator $\pi_S$ and let $\pi_S u_S = Q$ be a linear 
projection onto polynomila space $W_p$ such that $\mathbb{Q}_{s,t} \subset W_p$.

We can estimate interpolation error with the Bramble-Hilbert Lemma~\cite{PolynomialApproximation} 
and~\cite{EstimationofLinearFunctionals} in $H_r(\Omega)$ space, where $r = s$ in x direction and 
$r=t$ in y-direction.
%We use $\alpha$ for a two dimensional multi-index.
%\begin{lem}\label{lem:poincare}
%There exists a constant $C > 0$ such that
%\begin{equation}\label{eq:poincare}
%\norm{u}_{H^r_{\Omega}}^2 \leq C \Bigg\{\sum_{|\alpha| \leq r-1}\Big|\int_{\Omega} D^{\alpha}u \Big|^2 + |u|^2_{H^r(\Omega)} \Bigg\}
%\end{equation}
%\end{lem}
%We can derive this lemma with poincar\'e lemma and mathematical induction \cite{FETheory}.

\begin{lem}\label{lem:interpolation}
%Let $\Omega_S$ be the domain covered by stencil $S$ in $\mathbb{R}^2$ and let $\pi_S u = Q$ be a linear projection onto 
%polynomial space $W_p$ such that $\mathbb{Q}_{s,t} \subset W_p$, where $W_p$ is a subspace of $H^1(\Omega_S)$, 
%then there exists a constant $C > 0$, such that
Let $\Omega_S$ be the domain covered by stencil $S$ in $\mathbb{R}^2$ and let $u\in H^r(\Omega_S)$. We have: 
\begin{equation}
\norm{u-\pi_Su}^2 \leq h^{2(r-1)} |u|^2_{H^r(\Omega_S)}.
\end{equation}
\end{lem}

\begin{proof}
\begin{equation}
\norm{u-\pi_S u} = 
\end{equation}
\end{proof}

\subsection{Stencil polynomial smoothness indicators}
We can define a smoothness indicator $\sigma$ as a measurement of 
smoothness of $u(\mathbf{x})$ on the stencil $S$. 
The classic method from~\cite{JSSmoothness} is defined by Sobolev seminorm:
\begin{equation}
		  \sigma_P = \sum_{1\leq \lvert\alpha\rvert \leq m} 
		  \int\int_{E_0} \lvert E_0 \rvert^{\lvert\alpha\rvert -1} 
		  (\mathcal{D}^\alpha Q(x,y))^2 dxdy,
\end{equation}
where $\alpha = (\alpha_1, \alpha_2)$ is a multi-index, $\lvert \alpha \rvert = \alpha_1 + \alpha_2$, 
and $\mathcal{D}$ is the derivative operator $\mathcal{D}^\alpha = \partial^{\lvert \alpha \rvert} / \partial x^{\alpha_1} \partial y^{\alpha_2}$.
The Jiang-Shu smoothness indicator can be reformed with basis polynomials:
\begin{equation}


\end{equation}

We proposed two alternative ways of defining smoothness indicator
\cite{WENOAOSimplifiedSmoothness}.
The smoothness indicator should exhibit following convergence property that
there exists some value $C > 0$ such that $h \rightarrow 0^+$,
\begin{equation}\label{eq:smoothconvergence}
		  \sigma = \begin{cases}
					 Ch^2 + \mathcal{O} (h^3) \quad &\text{Smooth}, \\
					 \Theta (h)  &\text{Not Smooth}.
		  \end{cases}
\end{equation}


\subsection{ML-WENO weighting}
Given a target cell $E_{i,j} \in \mathbb{\tau}_h$, we can create a nonempty set of stencils $S_l \ni E_{i,j}, l = 0,1, \dots, L$.
Each stencil will 
\begin{figure}[ht]
\centering
\input chapters/mlstencil.tex
\caption{An example of (3,2,1) multi-level stencils combination on logically rectangular 
quadrilateral mesh.\label{fig:quadmesh}}
\end{figure}


\subsection{Boundary Treatment}

\subsection{Time Stepping}
