\section{Quadrilateral Mesh}
We consider a bounded computational domain $\Omega \subset \mathbb{R}^2$. 
Here, $\partial \Omega$ denotes the 
Lipschitz-continuous boundary of the domain.
Let $\mathcal{T}_h$ be a conforming mesh of quadrilaterals covering $\Omega$.
The quadrilateral $E \in \mathcal{T}_h$ is referred as element in finite element context, while
referred as cell in finite volume context.
Each quadrilateral $E$ is
assumed to be strictly convex, not degenerate into a triangle, line
or point. For ease of implementation, we use a logically rectangular mesh, 
where each element(cell) is indexed using 
Cartesian coordinates $\{i,j\}$. We show a reference element(cell) 
$\hat{E} = [-1,1]^2$,
an unscaled element $\tilde{E}$
and a scaled general quadrilateral element(cell) $E/H = \tilde{E}$ in 
Figure~\ref{fig:meshexample}.
There exists a non-affine bijective and smooth map $\mathbf{F}_{\hat{E}}: \hat{E} \rightarrow \tilde{E}$ mapping the vertices of $\hat{E}$ to
the vertices of $\tilde{E}$.
\begin{figure}[ht]
\centering
\input chapters/tikz/quadmesh.tex
\caption{An exhibition of a reference square and general non-degenerate quadrilateral element(cell) before and after scaling.}
\label{fig:meshexample}
\end{figure}

The edges of element $E$ are denoted by $e_i$, $i\in\{0,1,2,3\}$ in the counterclockwise direction.
We begin counting from zero, first to align with the indexing convention of C++, 
and second to facilitate representation in terms of modulo arithmetic, which
also start from zero.
The vertices are represented as $v_i = e_i \cap e_{i+1}$ 
, and $i\equiv i\Mod(4)$.
Here, $\pmb{\nu}_i$ and $\pmb{\tau}_i$ are used to represent the unit 
normal and the unit tangent
vector respectively. The unit normal vector $\pmb{\nu}_i$ points outerwards 
the quadrilateral. The tangent $\pmb{\tau}_i$ is generated by rotating 
$\pmb{\nu}_i$ counterclockwise by right-hand rule.

In Figure~\ref{fig:fullmesh}, we illustrate a full logically rectangular qusi-uniform quadrilateral mesh $\mathcal{T}_h$,
which is generated by randomly distort the interior vertex of a square mesh.
Note that vertex on the boundary $\partial \Omega$ remain untouched.
\begin{figure}[ht]
\centering
\input chapters/tikz/mesh.tex
\caption{An exhibition of a full logically rectangular qusi-uniform quadrilateral mesh $\mathcal{Q}_h$.}
\label{fig:fullmesh}
\end{figure}

\subsection{Shape Regularity}
We give an official definition of uniform shape regularity of a 
quadrilateral mesh.
\begin{defn}\label{def:shaperegularity}
For any quadrilateral $E \in \mathcal{T}_h$, we can get a set of four triangles
$T_{i}, \quad i \in \{0,1,2,3\}$. We form the triangle $T_i$ by excluding 
vertex $v_i$ from the quadrilaterl $E$, and use the remaining three vertex.
We define the following parameters as
\begin{equation}\label{eq:shaperegularity}
\begin{aligned}
		  h_E &= \text{diameter of E},\\
\rho_E &= 2 \min_{1\leq i\leq4} \{ \text{diameter of largest circle inscribed in} \hspace{0.2cm} T_i  \}.
\end{aligned}
\end{equation}
A mesh partion $\mathcal{T}_h$ is considered to be uniformly shape regular 
if there exists a shape regularity parameter $\sigma_*$
independent of $\mathcal{T}_h$,
such that the ratio 
\begin{equation}\label{eq:regularityineq}
\rho_E/h_E \geq \sigma_* >0,
\end{equation}
for all quadrilaterals $E \in \mathcal{T}_h$.
		  \end{defn}
